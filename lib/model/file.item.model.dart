class FileItemModel {
  String name;
  String path;
  DateTime createdDateTime;
  bool isSelected;
  FileItemModel({
    required this.name,
    required this.path,
    required this.createdDateTime,
    required this.isSelected,
  });
}
